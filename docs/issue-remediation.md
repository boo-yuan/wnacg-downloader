# 47 项问题修复记录

本记录按原审查建议顺序对应修复结果。阻断级问题没有跳过，已在本轮开始时重新验证。

| # | 问题 | 修复结果 |
|---:|---|---|
| 1 | 内部导入失效 | 统一为 `wnacg.*` 包导入。 |
| 2 | 数据库模块缺失 | 提供 SQLite 适配器、迁移、事务与恢复进度。 |
| 3 | 运行脚本目标错误 | `run.bat` 使用锁文件和 GUI entry point。 |
| 4 | 构建链失效 | 独立 build 依赖组、资源路径与包元数据复制。 |
| 5 | 缺少正式入口 | 提供 GUI script、`python -m wnacg` 和烟雾模式。 |
| 6 | TLS 验证关闭 | 所有 HTTP 客户端恢复证书验证。 |
| 7 | 更新源与完整性不可信 | 只检查 GitHub 官方 API，只打开经校验的官方发布页，不自动执行下载文件。 |
| 8 | 取消时可能删错目录 | 每个任务持久化下载根目录，删除前验证目标是其直属子目录。 |
| 9 | 同名漫画目录冲突 | 目录名包含稳定的 `[aid]`。 |
| 10 | 原始图片名冲突 | 新命名版本加入四位顺序前缀。 |
| 11 | 分页缺失仍完成 | 任一分页失败会使任务失败；已知图片数强制比对，未知时用浏览页数量验证直链数量。 |
| 12 | 下载中途读取可变配置 | 任务持久化不可变 `DownloadOptions` 快照。 |
| 13 | 迁移可能丢失备份 | 旧数据只复制缺失项，冲突保留并记录警告，不删除源备份。 |
| 14 | 响应体无上限 | 流式下载同时检查 `Content-Length` 和实际累计字节。 |
| 15 | ZIP 非原子 | 临时 ZIP 完整写入、`testzip()` 校验后原子替换。 |
| 16 | `with_suffix` 破坏含点目录名 | 使用显式同级归档路径函数。 |
| 17 | 配置无验证 | Pydantic Settings 对域名、代理、路径、范围和枚举执行验证。 |
| 18 | 单字段无效重置全部配置 | 逐字段恢复有效值，并备份损坏 JSON。 |
| 19 | 全局连接数实际为每任务限制 | 所有任务共享一个可动态调整的连接限制器。 |
| 20 | 增加任务上限不立即调度 | 设置回调立即唤醒队列并应用连接上限。 |
| 21 | GUI/异步热路径同步数据库 I/O | 下载协程使用 `asyncio.to_thread`，卡片查询使用有界 Qt 线程池。 |
| 22 | 每张卡创建原生线程 | 改为最大 4 线程的共享 `QThreadPool`。 |
| 23 | 封面池注释 5、实际 15 | 独立封面池固定为 5。 |
| 24 | 非 2xx 响应处理不一致 | 统一 `raise_for_status()`、有界重试和最终错误。 |
| 25 | 更新失败显示“已最新” | 失败、无更新、有更新分别展示。 |
| 26 | 日志查看器打开错误文件 | UI 使用日志模块导出的真实路径。 |
| 27 | 日志每次启动清空 | 改为追加、轮转和保留策略。 |
| 28 | 日志不是结构化格式 | 文件日志使用 JSON Lines。 |
| 29 | 取消对话框确认前改配置 | 只有用户确认后才保存删除文件选项。 |
| 30 | macOS `open` 空参数 | 跨平台路径打开统一交给 `QDesktopServices`。 |
| 31 | 镜像重试固定且污染全局状态 | 每次请求按去重后的配置镜像逐一尝试，不保留全局活动镜像。 |
| 32 | 同步/异步搜索解析重复 | 共用一个纯 HTML 解析器。 |
| 33 | 声称支持画廊 URL 但未实现 | 搜索框支持完整 URL 和 `aid:编号`，并解析画廊元数据。 |
| 34 | 预加载状态泄漏 | 成功、失败和过期结果都会清除对应预加载键。 |
| 35 | UI 与下载器的路径/完成判断不一致 | 共用任务目录与归档路径函数，优先使用持久化任务路径。 |
| 36 | 缺少清晰端口边界 | 引入 `TaskRepository` 端口和 SQLite 适配器，下载器支持依赖注入。 |
| 37 | 导入即执行外部恢复操作 | 数据库初始化与下载状态恢复移到显式启动阶段，封面缓存延迟创建。 |
| 38 | 大量类型缺失 | 全项目 Pyright 检查，领域、应用与关键基础设施使用 strict 模式。 |
| 39 | 没有严格类型配置 | `pyproject.toml` 提供 Python 3.12 Pyright 配置。 |
| 40 | Ruff/提交钩子缺失 | Ruff 纳入 dev 组并提供 Ruff + Pyright pre-commit。 |
| 41 | 没有测试 | 添加领域、路径、配置恢复、数据库、抓取解析、文件事务和更新器测试。 |
| 42 | 没有 CI | Windows CI 执行锁定安装、Ruff、Pyright、pytest 和 Qt 烟雾测试。 |
| 43 | 文档与模块边界不足 | 添加 README、模块文档，并抽取路径、端口和平台打开模块。 |
| 44 | 版本来源不统一 | 运行时从安装包元数据读取 `pyproject.toml` 版本，构建时复制元数据。 |
| 45 | 直接/冗余依赖不正确 | 保留实际直接依赖，移除未使用的 qasync、SQLModel 和 aiosqlite。 |
| 46 | 主入口格式和职责混乱 | 入口只负责初始化、Qt 生命周期和烟雾模式。 |
| 47 | 领域不变量不足 | 校验 aid、计数边界、不可变任务选项和合法状态迁移。 |

## 验证门禁

- `uv lock --check`
- `ruff check` 与 `ruff format --check`
- Pyright：0 errors
- pytest：19 passed
- 源码 Qt 离屏烟雾测试
- `run.bat --smoke-test`
- PyInstaller 单文件构建
- 生成的 EXE `--smoke-test`：退出码 0
